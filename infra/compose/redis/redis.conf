bind 0.0.0.0
protected-mode yes
port 6379
daemonize no
logfile ""
maxmemory 256mb
maxmemory-policy noeviction
# TTCP-003 has no jobs to recover. Durable job state belongs in PostgreSQL.
save ""
appendonly no
dir /data
aclfile /run/redis/users.acl
