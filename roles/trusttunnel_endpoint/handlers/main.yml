---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart TrustTunnel
  ansible.builtin.systemd:
    name: "{{ trusttunnel_service_name }}"
    state: restarted
    daemon_reload: true
